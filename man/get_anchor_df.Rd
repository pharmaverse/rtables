% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nesting_impl.R
\name{get_full_lyt_df}
\alias{get_full_lyt_df}
\alias{get_full_lyt_df,PreDataTableLayouts-method}
\alias{get_layout_dfs}
\alias{get_full_lyt_df,PreDataRowLayout-method}
\alias{get_full_lyt_df,PreDataColLayout-method}
\alias{get_full_lyt_df,SplitVector-method}
\alias{get_full_lyt_df,SplitVectorTree-method}
\alias{get_full_lyt_df,Split-method}
\alias{get_full_lyt_df,VTableNodeInfo-method}
\alias{get_anchor_dfs}
\alias{get_row_anchor_df}
\alias{get_row_anchor_list}
\title{Retrieve Info About Possible Nesting Anchors}
\usage{
get_full_lyt_df(
  splvec,
  next_node = 1L,
  next_anchor_step = 1L,
  parent,
  depth,
  node_type
)

\S4method{get_full_lyt_df}{PreDataTableLayouts}(
  splvec,
  next_node = 1L,
  next_anchor_step = 1,
  parent,
  depth,
  node_type
)

get_layout_dfs(lyt)

\S4method{get_full_lyt_df}{PreDataRowLayout}(
  splvec,
  next_node = 1,
  next_anchor_step = 1L,
  parent = 0L,
  depth = 1L,
  node_type = "active"
)

\S4method{get_full_lyt_df}{PreDataColLayout}(
  splvec,
  next_node = 1,
  next_anchor_step = 1L,
  parent = 0L,
  depth = 1L,
  node_type = "active"
)

\S4method{get_full_lyt_df}{SplitVector}(
  splvec,
  next_node = 1L,
  next_anchor_step = 1L,
  parent,
  depth,
  node_type
)

\S4method{get_full_lyt_df}{SplitVectorTree}(
  splvec,
  next_node = 1L,
  next_anchor_step = 1L,
  parent,
  depth,
  node_type
)

\S4method{get_full_lyt_df}{Split}(
  splvec,
  next_node = 1L,
  next_anchor_step = 1L,
  parent,
  depth,
  node_type
)

\S4method{get_full_lyt_df}{VTableNodeInfo}(
  splvec,
  next_node = 1L,
  next_anchor_step = 1L,
  parent,
  depth,
  node_type
)

get_anchor_dfs(lyt)

get_row_anchor_df(lyt)

get_row_anchor_list(lyt)
}
\arguments{
\item{splvec}{(\code{PreDataTableLayouts} or internal classes)\cr The layout or partial
layout to list anchors for.}

\item{next_node}{(\code{integer(1)})\cr For internal use}

\item{next_anchor_step}{(\code{integer(1)})\cr For internal use.}

\item{parent}{(\code{integer(1)})\cr For internal use.}

\item{depth}{(\code{integer(1)})\cr For internal use.}

\item{node_type}{(\code{character(1)})\cr For internal use.}

\item{lyt}{(\code{PreDataTableLayouts} or \code{PreDataRowLayout})\cr A layout or row
to identify anchors for.}
}
\value{
for \code{get_layout_dfs} a list with \code{rows} and \code{cols} elements
containing layout data.frames (see Details) for each structural
dimension; \code{get_anchor_dfs} returns the same, but with anchor
data.frames rather than full layout ones. \code{get_anchor_row_df}
is a convenience function that returns only the \code{rows} anchor
data.frame. \code{get_row_anchor_list} returns a list, each element
of which is a set of the names of one or more layout
instructions that will be placed as direct siblings to
each-other; i.e., anchored to the first element of the vector
when the length of the element is greater than one.
}
\description{
This function scans an existing layout's row structure and lists
valid \code{at_sibling} anchors for intermediate nesting.
}
\details{
A layout data.frame is a data.frame describing a single dimension
of pre-data layout structure, containing the following columns
(most of which are used for internal implementations and will not
be useful to the end-user):
\itemize{
\item \code{name}: name of the element
\item \code{nodeid}: sequential numeric id of the node, for use in
constructing graphs (0 is the root node)
\item \code{parentid}: node id of the layout instructions direct parent
\item \code{depth}: length of path from root to the current node through the
implicit graph defined by the (\code{nodeid}, \code{parentid}) pairings
\item \code{type}: a description of the 'type' of the node, used internally
\item \code{is_toplevel}: whether the node represents a top-level split/analysis.
\item \code{anchor_step}: position \emph{along the path of eligible anchor
points}, (\code{NA} for nodes not along that path).
}

The core difference between a layout data.frame and an anchor
data.frame is that the anchor df has been subset to remove rows for
nodes not currently eligible to be anchor points (ie allowed
targets for a subsequent instruction's \code{at_sibling} argument).
}
\note{
Instructions which are anchored in such a way that they
ultimately become top-level instructions in the layout (i.e.,
by being anchored as a sibling to a top-level instruction) are
handled somewhat differently for implementation reasons and may
present differently to those anchored to non-top-level
instructions.
}
\examples{

lyt <- basic_table() |>
  split_cols_by("ARM") |>
  split_rows_by("STRATA1") |>
  split_rows_by("RACE") |>
  split_rows_by("SEX") |>
  analyze("AGE") |>
  split_rows_by("BMRKR1", at_sibling = "RACE") |>
  analyze("AGE")

get_layout_dfs(lyt)
get_anchor_dfs(lyt)
get_row_anchor_df(lyt)

}
